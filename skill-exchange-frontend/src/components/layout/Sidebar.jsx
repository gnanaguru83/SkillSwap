import { NavLink } from 'react-router-dom';
import { X } from 'lucide-react';
const links = [['/dashboard','Dashboard'],['/discover','Discover'],['/matches','Matches'],['/sessions','Sessions'],['/chat','Chat'],['/profile','Profile']];
export default function Sidebar({ open, onClose }) { return <div className={`fixed inset-0 z-50 md:hidden ${open ? '' : 'pointer-events-none'}`}><div onClick={onClose} className={`absolute inset-0 bg-gray-900/40 transition ${open ? 'opacity-100' : 'opacity-0'}`}/><aside className={`absolute left-0 top-0 h-full w-72 bg-white p-5 shadow-2xl transition ${open ? 'translate-x-0' : '-translate-x-full'}`}><div className="mb-6 flex items-center justify-between"><span className="font-display text-2xl font-extrabold">SkillSwap</span><button onClick={onClose}><X/></button></div><nav className="space-y-2">{links.map(([to,label]) => <NavLink key={to} onClick={onClose} to={to} className={({isActive}) => `block rounded-xl px-4 py-3 font-bold ${isActive ? 'bg-purple-50 text-purple-700' : 'text-gray-600 hover:bg-gray-50'}`}>{label}</NavLink>)}</nav></aside></div>; }